@baseUrl = http://localhost:3001
@authToken = Bearer <token>

#------------------------------------------------ Auth ---------------------------------------------------
### verify if the token is valid
GET {{baseUrl}}/api/auth/verify
Authorization: {{authToken}}

#-----------------------------------------------Analytics -----------------------------------------------
### get all the aggregated data fro admin dashboard
GET {{baseUrl}}/api/analytic/dashboard-admin
Authorization: {{authToken}}

#-----------------------------------------------Uploads --------------------------------------------------
### upload an image 
### send as form data with file name as image
### use post man 
POST {{baseUrl}}/api/upload/image
Authorization: {{authToken}}

#-----------------------------------------------User-------------------------------------------------------
### get user details by id
### some details are not returned for normal users
GET {{baseUrl}}/api/user/{id}
Authorization: {{authToken}}

### get users by filter this requires admin access 
## can filter based on role , name 
POST {{baseUrl}}/api/user/filter
Authorization: {{authToken}}
Content-Type: application/json

{
    "role":"user",
    "searchText":"Marco"
}